{
  "info": {
    "_postman_id": "7f16ce0d-d600-43ec-9645-ff2bf44726de",
    "name": "INF781-ExamenFinal",
    "description": "Coleccion Parte 4 SecureWallet: 10 peticiones OWASP RS-01 a RS-10. Usa CAPTCHA_TEST_TOKEN solo en ambiente local/testing.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Parte 4 - Requerimientos de Seguridad RS-01 a RS-10",
      "item": [
        {
          "name": "01 RS BOLA/IDOR - 404 transferencia ajena bloqueada",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "setEnv('postmanTestRun', 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2));",
                  "login('userAEmail', 'userAPassword', 'userAToken', 'refreshToken', function () {",
                  "  login('userBEmail', 'userBPassword', 'userBToken', 'userBRefreshToken', function () {",
                  "    login('adminEmail', 'adminPassword', 'adminToken', 'adminRefreshToken', function () {",
                  "      const userBToken = pm.environment.get('userBToken');",
                  "      pm.sendRequest({",
                  "        url: baseUrl() + '/wallet/topup',",
                  "        method: 'POST',",
                  "        header: requestHeaders({ Authorization: 'Bearer ' + userBToken }),",
                  "        body: { mode: 'raw', raw: JSON.stringify({ monto: 100, descripcion: 'Setup RS-01 BOLA' }) }",
                  "      }, function (errTopup, resTopup) {",
                  "        if (errTopup || !resTopup || ![200, 201].includes(resTopup.code)) return failSetup('No se pudo recargar USER B para RS-01.', resTopup);",
                  "        const key = uuidv4();",
                  "        pm.sendRequest({",
                  "          url: baseUrl() + '/transfers',",
                  "          method: 'POST',",
                  "          header: requestHeaders({ Authorization: 'Bearer ' + userBToken, 'Idempotency-Key': key }),",
                  "          body: { mode: 'raw', raw: JSON.stringify({ destinatario: env('userAEmail', ''), monto: 20, descripcion: 'Transferencia ajena para RS-01' }) }",
                  "        }, function (errTransfer, resTransfer) {",
                  "          if (errTransfer || !resTransfer || resTransfer.code !== 201) return failSetup('No se pudo crear transferencia ajena para RS-01.', resTransfer);",
                  "          const transfer = jsonOf(resTransfer);",
                  "          if (!transfer.uuid) return failSetup('La transferencia RS-01 no devolvio uuid.', resTransfer);",
                  "          setEnv('transferUuid', transfer.uuid);",
                  "        });",
                  "      });",
                  "    });",
                  "  });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-01 estado correcto 403 o 404', function () {",
                  "  pm.expect([403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('RS-01 no expone saldo, wallet, movimientos ni datos financieros ajenos', function () {",
                  "  const text = pm.response.text().toLowerCase();",
                  "  ['wallet', 'saldo', 'balance', 'balance_after', 'amount_cents', 'movimientos', 'transactions'].forEach(function (term) {",
                  "    pm.expect(text, 'No debe contener ' + term).to.not.include(term);",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userAToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/transfers/{{transferUuid}}/confirm",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "transfers",
                "{{transferUuid}}",
                "confirm"
              ]
            },
            "description": "USER A intenta confirmar una transferencia creada por USER B manipulando el UUID. Resultado correcto: 403 o 404.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"confirmed\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "02 RS RBAC - 403 USER no accede a /admin/users",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "ensureToken('userAToken', 'userAEmail', 'userAPassword', 'refreshToken');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-02 estado correcto 403', function () {",
                  "  pm.expect(pm.response.code).to.eql(403);",
                  "});",
                  "pm.test('RS-02 respuesta JSON desde backend', function () {",
                  "  pm.expect((pm.response.headers.get('Content-Type') || '').toLowerCase()).to.include('application/json');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userAToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/users",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "users"
              ]
            },
            "description": "USER intenta consumir endpoint ADMIN. Resultado correcto: 403."
          }
        },
        {
          "name": "03 RS Criptografia - 200 no expone secretos",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "ensureToken('userAToken', 'userAEmail', 'userAPassword', 'refreshToken');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-03 estado correcto 200', function () {",
                  "  pm.expect(pm.response.code).to.eql(200);",
                  "});",
                  "pm.test('RS-03 no expone password, hashes, MFA secret ni tokens', function () {",
                  "  const text = pm.response.text().toLowerCase();",
                  "  ['password', 'password_hash', 'mfa_secret', 'totp_secret', 'refresh_token', 'token_hash'].forEach(function (term) {",
                  "    pm.expect(text, 'No debe contener ' + term).to.not.include(term);",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userAToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "me"
              ]
            },
            "description": "Perfil autenticado sin secretos. Resultado correcto: 200."
          }
        },
        {
          "name": "04 RS Injection - 422 datos manipulados rechazados",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "ensureToken('userAToken', 'userAEmail', 'userAPassword', 'refreshToken');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-04 estado correcto 422', function () {",
                  "  pm.expect(pm.response.code).to.eql(422);",
                  "});",
                  "pm.test('RS-04 no muestra SQL ni stack trace', function () {",
                  "  const text = pm.response.text().toLowerCase();",
                  "  ['select ', 'insert ', 'update ', 'delete ', 'sqlstate', 'stack trace', 'exception'].forEach(function (term) {",
                  "    pm.expect(text, 'No debe contener ' + term).to.not.include(term);",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userAToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wallet/topup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wallet",
                "topup"
              ]
            },
            "description": "Body manipulado con monto invalido y campos extra role, balance_cents, wallet_id. Resultado correcto: 422.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"monto\": \"1 OR 1=1\",\n  \"descripcion\": \"Intento injection mass assignment\",\n  \"role\": \"ADMIN\",\n  \"balance_cents\": 999999999,\n  \"wallet_id\": 1\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "05 RS Idempotencia - 201 no duplica transferencia",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "ensureToken('userAToken', 'userAEmail', 'userAPassword', 'refreshToken', function (token) {",
                  "  const key = uuidv4();",
                  "  setEnv('idempotencyKey', key);",
                  "  setEnv('rs05Body', JSON.stringify({ destinatario: env('userBEmail', ''), monto: 15, descripcion: 'RS-05 transferencia idempotente' }));",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/wallet/topup',",
                  "    method: 'POST',",
                  "    header: requestHeaders({ Authorization: 'Bearer ' + token }),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ monto: 100, descripcion: 'Setup RS-05' }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || ![200, 201].includes(res.code)) return failSetup('No se pudo recargar USER A para RS-05.', res);",
                  "  });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const first = pm.response.json();",
                  "pm.test('RS-05 primera transferencia devuelve 201', function () {",
                  "  pm.expect(pm.response.code).to.eql(201);",
                  "});",
                  "pm.test('RS-05 primera respuesta trae uuid', function () {",
                  "  pm.expect(first).to.have.property('uuid');",
                  "  pm.environment.set('rs05FirstUuid', first.uuid);",
                  "});",
                  "pm.sendRequest({",
                  "  url: pm.environment.get('baseUrl').replace(/[/]$/, '') + '/transfers',",
                  "  method: 'POST',",
                  "  header: {",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    Authorization: 'Bearer ' + pm.environment.get('userAToken'),",
                  "    'Idempotency-Key': pm.environment.get('idempotencyKey'),",
                  "    'X-Postman-Test-Run': pm.environment.get('postmanTestRun') || ''",
                  "  },",
                  "  body: { mode: 'raw', raw: pm.environment.get('rs05Body') }",
                  "}, function (err, res) {",
                  "  pm.test('RS-05 repetida con misma Idempotency-Key devuelve 200 o 201', function () {",
                  "    pm.expect(err).to.eql(null);",
                  "    pm.expect([200, 201]).to.include(res.code);",
                  "  });",
                  "  pm.test('RS-05 repetida devuelve el mismo uuid', function () {",
                  "    pm.expect(res.json().uuid).to.eql(pm.environment.get('rs05FirstUuid'));",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userAToken}}",
                "type": "text"
              },
              {
                "key": "Idempotency-Key",
                "value": "{{idempotencyKey}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/transfers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "transfers"
              ]
            },
            "description": "Crea transferencia y repite misma Idempotency-Key dentro del test. Resultado correcto: mismo uuid.",
            "body": {
              "mode": "raw",
              "raw": "{{rs05Body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "06 RS Configuracion segura - 200 CORS y cabeceras",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-06 estado correcto 200', function () {",
                  "  pm.expect(pm.response.code).to.eql(200);",
                  "});",
                  "pm.test('RS-06 CORS no permite origen malicioso', function () {",
                  "  pm.expect(pm.response.headers.get('Access-Control-Allow-Origin')).to.not.eql('http://evil.example');",
                  "});",
                  "pm.test('RS-06 cabeceras de seguridad presentes', function () {",
                  "  pm.expect(pm.response.headers.get('X-Content-Type-Options')).to.eql('nosniff');",
                  "  pm.expect(pm.response.headers.get('X-Frame-Options')).to.eql('DENY');",
                  "  pm.expect(pm.response.headers.get('Content-Security-Policy')).to.exist;",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Origin",
                "value": "http://evil.example",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/health",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "health"
              ]
            },
            "description": "Origin malicioso y revision de headers. Resultado correcto: 200 sin CORS malicioso y con headers."
          }
        },
        {
          "name": "07 RS Refresh token rotativo - 401 reuso bloqueado",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "function rotateOldRefresh() {",
                  "  const oldRefresh = pm.environment.get('refreshToken');",
                  "  if (!oldRefresh) return login('userAEmail', 'userAPassword', 'userAToken', 'refreshToken', rotateOldRefresh);",
                  "  setEnv('oldRefreshToken', oldRefresh);",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/refresh',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ refresh_token: oldRefresh }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('El primer refresh de preparacion RS-07 debe ser 200.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) return failSetup('El primer refresh no devolvio tokens nuevos.', res);",
                  "    setEnv('userAToken', data.tokens.access_token);",
                  "    setEnv('refreshToken', data.tokens.refresh_token);",
                  "  });",
                  "}",
                  "rotateOldRefresh();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-07 estado correcto 401 al reutilizar refresh viejo', function () {",
                  "  pm.expect(pm.response.code).to.eql(401);",
                  "});",
                  "pm.test('RS-07 no devuelve nuevos tokens ante reuso', function () {",
                  "  const text = pm.response.text().toLowerCase();",
                  "  pm.expect(text).to.not.include('access_token');",
                  "  pm.expect(text).to.not.include('refresh_token');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Postman-Test-Run",
                "value": "{{postmanTestRun}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/refresh",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "refresh"
              ]
            },
            "description": "El pre-request hace un refresh 200. La peticion reutiliza el refresh viejo. Resultado correcto: 401.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refresh_token\": \"{{oldRefreshToken}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "08 RS Limit login - 429 sexto intento bloqueado",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "const email = 'rate-' + Date.now() + '-' + Math.random().toString(16).slice(2) + '@invalid.test';",
                  "setEnv('rateLimitEmail', email);",
                  "const badBody = JSON.stringify({ email: email, password: 'Password_erronea_123!', captcha_token: captchaToken() });",
                  "function hit(n) {",
                  "  if (n <= 0) return;",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: badBody }",
                  "  }, function () { hit(n - 1); });",
                  "}",
                  "hit(5);"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-08 estado correcto 429', function () {",
                  "  pm.expect(pm.response.code).to.eql(429);",
                  "});",
                  "pm.test('RS-08 incluye Retry-After o retry_after', function () {",
                  "  let body = {};",
                  "  try { body = pm.response.json(); } catch (e) {}",
                  "  const hasHeader = !!pm.response.headers.get('Retry-After');",
                  "  const hasBody = Object.prototype.hasOwnProperty.call(body, 'retry_after');",
                  "  pm.expect(hasHeader || hasBody).to.eql(true);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Postman-Test-Run",
                "value": "{{postmanTestRun}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "login"
              ]
            },
            "description": "El pre-request envia 5 intentos fallidos y esta peticion es el sexto. Resultado correcto: 429.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{rateLimitEmail}}\",\n  \"password\": \"Password_erronea_123!\",\n  \"captcha_token\": \"{{captchaToken}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "09 RS Auditoria - ADMIN 200 y user denegado",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function env(name, fallback) {",
                  "  const value = pm.environment.get(name);",
                  "  return value !== undefined && value !== null && String(value) !== '' ? value : fallback;",
                  "}",
                  "",
                  "function setEnv(name, value) {",
                  "  pm.environment.set(name, value || '');",
                  "}",
                  "",
                  "function baseUrl() {",
                  "  return env('baseUrl', 'http://127.0.0.1:8000/api/v1').replace(/\\/$/, '');",
                  "}",
                  "",
                  "function captchaToken() {",
                  "  return env('captchaToken', 'postman-test-captcha');",
                  "}",
                  "",
                  "function runId() {",
                  "  let id = pm.environment.get('postmanTestRun');",
                  "  if (!id) {",
                  "    id = 'run-' + Date.now() + '-' + Math.random().toString(16).slice(2);",
                  "    setEnv('postmanTestRun', id);",
                  "  }",
                  "  return id;",
                  "}",
                  "",
                  "function uuidv4() {",
                  "  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {",
                  "    const r = Math.random() * 16 | 0;",
                  "    const v = c === 'x' ? r : (r & 0x3 | 0x8);",
                  "    return v.toString(16);",
                  "  });",
                  "}",
                  "",
                  "function jsonOf(res) {",
                  "  try { return res.json(); } catch (e) { return {}; }",
                  "}",
                  "",
                  "function requestHeaders(extra) {",
                  "  return Object.assign({",
                  "    Accept: 'application/json',",
                  "    'Content-Type': 'application/json',",
                  "    'X-Postman-Test-Run': runId()",
                  "  }, extra || {});",
                  "}",
                  "",
                  "function failSetup(message, res) {",
                  "  const detail = res ? (' HTTP ' + res.code + ' ' + res.text()) : '';",
                  "  throw new Error(message + detail);",
                  "}",
                  "",
                  "function login(emailVar, passwordVar, tokenVar, refreshVar, cb) {",
                  "  const email = env(emailVar, '');",
                  "  const password = env(passwordVar, '');",
                  "  if (!email || !password) failSetup('Faltan credenciales para ' + emailVar + '.');",
                  "",
                  "  pm.sendRequest({",
                  "    url: baseUrl() + '/auth/login',",
                  "    method: 'POST',",
                  "    header: requestHeaders(),",
                  "    body: { mode: 'raw', raw: JSON.stringify({ email, password, captcha_token: captchaToken() }) }",
                  "  }, function (err, res) {",
                  "    if (err || !res || res.code !== 200) return failSetup('No se pudo iniciar sesion para ' + email + '.', res);",
                  "    const data = jsonOf(res);",
                  "    if (!data.tokens || !data.tokens.access_token || !data.tokens.refresh_token) {",
                  "      return failSetup('Login sin tokens para ' + email + '. Revisa MFA/CAPTCHA.', res);",
                  "    }",
                  "    setEnv(tokenVar, data.tokens.access_token);",
                  "    if (refreshVar) setEnv(refreshVar, data.tokens.refresh_token);",
                  "    cb && cb(data.tokens);",
                  "  });",
                  "}",
                  "",
                  "function ensureToken(tokenVar, emailVar, passwordVar, refreshVar, cb) {",
                  "  const token = pm.environment.get(tokenVar);",
                  "  if (token) return cb && cb(token);",
                  "  login(emailVar, passwordVar, tokenVar, refreshVar, function (tokens) { cb && cb(tokens.access_token); });",
                  "}",
                  "",
                  "ensureToken('adminToken', 'adminEmail', 'adminPassword', 'adminRefreshToken');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-09 ADMIN estado correcto 200', function () {",
                  "  pm.expect(pm.response.code).to.eql(200);",
                  "});",
                  "pm.test('RS-09 ADMIN recibe data con campos de auditoria', function () {",
                  "  const json = pm.response.json();",
                  "  pm.expect(json.data).to.be.an('array');",
                  "  pm.expect(json.data.length).to.be.greaterThan(0);",
                  "  const log = json.data[0];",
                  "  ['action', 'ip', 'user_agent', 'created_at'].forEach(function (field) {",
                  "    pm.expect(log).to.have.property(field);",
                  "  });",
                  "});",
                  "pm.sendRequest({",
                  "  url: pm.environment.get('baseUrl').replace(/[/]$/, '') + '/admin/audit-logs?per_page=1',",
                  "  method: 'GET',",
                  "  header: { Accept: 'application/json', Authorization: 'Bearer ' + pm.environment.get('userBToken') }",
                  "}, function (err, res) {",
                  "  pm.test('RS-09 USER no puede ver auditoria', function () {",
                  "    pm.expect(err).to.eql(null);",
                  "    pm.expect(res.code).to.eql(403);",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/audit-logs?per_page=20",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "audit-logs?per_page=20"
              ]
            },
            "description": "ADMIN consulta auditoria y el test interno valida que USER recibe 403. Resultado correcto: ADMIN 200."
          }
        },
        {
          "name": "10 RS XSS/CSRF - 401 cookies falsas no autentican",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('RS-10 estado correcto 401', function () {",
                  "  pm.expect(pm.response.code).to.eql(401);",
                  "});",
                  "pm.test('RS-10 respuesta JSON, no HTML ejecutable', function () {",
                  "  const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "  const text = pm.response.text().toLowerCase();",
                  "  pm.expect(contentType).to.include('application/json');",
                  "  pm.expect(text).to.not.include('<script');",
                  "  pm.expect(text).to.not.include('<!doctype');",
                  "});",
                  "pm.test('RS-10 no emite cookies de tokens', function () {",
                  "  const setCookie = (pm.response.headers.get('Set-Cookie') || '').toLowerCase();",
                  "  pm.expect(setCookie).to.not.include('access_token');",
                  "  pm.expect(setCookie).to.not.include('refresh_token');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Cookie",
                "value": "access_token={{adminToken}}; refresh_token={{adminRefreshToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "me"
              ]
            },
            "description": "Ruta protegida solo con cookies falsas, sin Authorization Bearer. Resultado correcto: 401."
          }
        }
      ]
    }
  ]
}
